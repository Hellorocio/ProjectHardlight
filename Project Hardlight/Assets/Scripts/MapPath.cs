using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Give it endpoints and its children that have MapPathNode script make up the path
public class MapPath : MonoBehaviour
{
    public MapLocation startNode;
    public MapLocation endNode;
}
