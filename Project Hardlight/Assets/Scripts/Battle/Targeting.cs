using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Targeting : MonoBehaviour
{
    public enum Type { Unknown, TargetUnit, TargetPosition, Radius, Instant };
}
